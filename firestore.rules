rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read/write for the entire database as requested for 'libre' access
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
